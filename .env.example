# ── Local dev only (VITE_ keys are NOT used in production) ──
VITE_LLM_PROVIDER=gemini
VITE_GEMINI_API_KEY=
VITE_GROQ_API_KEY=

# ── Vercel environment variables (set in dashboard, NOT here) ──
# GEMINI_API_KEY=
# GROQ_API_KEY=
# LLM_PROVIDER=gemini
